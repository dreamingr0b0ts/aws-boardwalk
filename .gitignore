# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json

# Lambda bundles
infra/build/

# Environment / secrets
.env
.env.*
*.pem

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Demo credentials (never committed) + runtime frontend config (written at publish time)
.demo-creds
*/frontend/config.json

# IDP plank: locally generated seed PDFs
doc-processing/corpus/pdfs/

# API plank: rendered spec is written at publish time
api-platform/frontend/openapi.json

# Plank 8 report Lambda archive (built by terraform archive provider)
security-posture/demo/lambda/report.zip

# Plank 9 report Lambda archive (built by terraform archive provider)
network-blueprint/demo/lambda/report.zip

# Plank 11 Lambda archives (built by terraform archive provider)
relational-registry/infra/lambda/query.zip
relational-registry/demo/lambda/seed.zip
relational-registry/demo/lambda/report.zip
